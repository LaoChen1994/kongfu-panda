# 项目开发规则

## 设计基线

- 在进行任何游戏设计、代码实现、美术资源、音频、数值或测试工作前，必须先阅读 `docs/GAME_DESIGN_DOCUMENT.md` 的相关章节。
- `docs/GAME_DESIGN_DOCUMENT.md` 是本项目唯一的游戏设计基线。
- 用户最新的明确要求优先于 GDD；如果新要求与 GDD 冲突，必须先指出冲突、影响范围和建议，再获得确认。
- 不得自行新增角色、系统、货币、装备槽、地图、联机功能或局外养成。
- 经确认的设计变化必须同步修改 GDD，并追加版本记录。

## MVP 范围

- MVP 固定为桌面浏览器、俯视角 2D、Phaser + TypeScript + Vite。
- MVP 固定包含 3 只熊猫、1 张地图、8 件武器、24 件道具、10 种普通敌人、2 种精英变体、1 个最终 Boss 和 10 波战斗。
- MVP 不包含多人联机、完整移动端适配、永久属性树、装备随机词缀、在线账号和排行榜。
- 如任务超出以上范围，先说明扩展成本并请求确认。

## 技术边界

- Simulation 负责战斗规则、实体状态、计时、波次、升级、经济和可保存状态。
- Phaser Renderer 负责场景、动画、摄像机、粒子和输入采集，不作为业务状态真相来源。
- HUD、升级、商城、暂停和结算优先使用 DOM/CSS。
- 角色、武器、道具、敌人和波次使用稳定 ID 与集中数据配置。
- 不得把伤害、经验、经济和波次规则散落在场景回调或 UI 组件中。
- 存档只保存可序列化状态，不保存 Phaser 对象。

## 变更与验证

- 所有可玩功能、游戏 UI、视觉反馈和运行时缺陷修复必须使用 `.agents/skills/game-development-loop/SKILL.md`，并遵循 `docs/AGENT_LOOP.md`。
- 完成条件必须包含设计验收、自动检查、真实浏览器试玩、当前代码截图、预期对比和最后修复后的回归验证。
- 本地截图和验证笔记保存到 `artifacts/agent-loop/<task>/iteration-<nn>/`，不得用旧截图证明新代码。
- 只实现当前任务要求的最小范围，不提前实现后续里程碑内容。
- 每项改动都应能追溯到 GDD 章节或用户已确认的新要求。
- 实现玩法时同时验证正常流程、失败流程、暂停/恢复和边界输入。
- 涉及视觉表现时必须验证战场可读性，危险预警优先于粒子和伤害数字。
- 涉及随机系统时保留可复现随机种子。
- 始终使用中文沟通、计划和反馈。
